name: 01 - Building Blocks
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: echo "Hello, World!"
  echo_goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Sucsess step
        run: echo "I will fail" && exit 0
      - name: Say Goodbye
        run: echo "Goodbye"
      